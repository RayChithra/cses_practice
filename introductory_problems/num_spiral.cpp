#include <iostream>

using namespace std;

int main()
{
    long long int t, y, x, ctr, row, col;
    cin >> t;
    for(int cnt = 0; cnt < t; cnt++)
    {
        cin >> y >> x;
        ctr = 0;
        row = 0;
        col = 0;
        while ((row != (x-1)) && (col != (y-1)))
        {
            
        }
    }
}